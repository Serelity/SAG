# Optional Linux/NVIDIA inference backend. Install in an isolated Conda environment
# after confirming the server CUDA driver is compatible with the selected wheel.
-r requirements.entity.txt
vllm>=0.8.5,<0.10.0
